# SmartLab 2.0 项目目录说明

一句话原则：仓库根目录把「设备适配器 / 后端 / 前端 / 文档 / 联调夹具」拆开；后端 Java 按 `adapter`（MQTT 设备桥）、`engine`（运行时）、`global`（契约与基础设施）、`management`（API 与落库）四层并列。大模型生成工作流模型文件若要新建包，应与这四层同级叫 `agent`，不要叫 `model`（已与设备模型、流程模型、约束模型重名）。

---

## 仓库根目录

- `adapter/`：实验室物理设备侧的独立适配器工程（C++/Python），经 MQTT 与后端通信，不是 Java 包。
- `Backend/`：Spring Boot 后端，提供 REST、编译执行、约束、MQTT 桥和 PostgreSQL 访问。
- `Frontend/`：Vue 3 管理端，覆盖设备、流程设计、任务运行、约束、数据和用户。
- `docs/`：架构说明、设计稿与实现计划。
- `fixture/`：本地联调用的模拟设备和适配器脚本。
- `.agents/`：本仓库可用的 Cursor/Agent 技能说明。
- `README.md`：仓库总览与启动说明。
- `项目目录说明.md`：本文件，按目录和 Java 源文件逐项一句话说明。
- `PERMISSION_INFO.xlsx` / `菜单权限映射_最终.csv`：菜单与权限种子数据。
- `数据库表结构.xlsx` / `智能实验室系统数据库结构说明.docx`：库表说明。
- `智能实验室系统软件架构文档.md` / `系统ui设计规范.md` / `设备管理业务方案设计.md`：论文/方案类文档。
- `USER_MANAGEMENT_UI_REFACTOR_PLAN.md` / `约束引擎与约束管理优化方案.md`：专项改造说明。
- `A Task-Oriented Executable Modeling Framework for Smart Chemical Laboratory.pdf`：相关论文 PDF。

---

## adapter/（设备侧，非 Java）

- `adapter/camera/`：相机/RTSP 相关适配脚本。
- `adapter/edge-agent-runtime/`：边缘 Agent 运行时（Python）及配置样例。
- `adapter/GetPLCData/`：PLC 数据采集 C++ 工程。
- `adapter/MotionStageController/`：运动台控制 C++ 工程。
- `adapter/ValueAdapterMQTT/`：PLC 数值经 MQTT 上报的 C++ 工程。
- `adapter/testAdapter/`：Python 测试适配器与 Codesys/PLC 辅助文件。

---

## fixture/

- `fixture/adapter/`：联调用的模拟适配器运行时。
- `fixture/device/`：联调用的模拟设备（如温度传感器）。
- `fixture/run_adapter.py` / `fixture/run_device.py`：启动夹具的入口脚本。
- `fixture/README.md`：夹具用法说明。

---

## docs/

- `docs/architecture/`：协议到运行时实现链等架构文档。
- `docs/mockups/`：界面色稿/线框类 HTML。
- `docs/superpowers/plans/`：按日期的实现计划。
- `docs/superpowers/specs/`：按日期的设计规格。
- `docs/software-architecture-and-project-plan.md`：软件架构与项目规划总稿。
- `docs/workflow-engine-development-plan.md`：工作流引擎开发计划。
- `docs/系统ui设计规范.md`：UI 规范副本。
- `docs/codex-execution-plan-outline.md`：执行计划提纲。

---

## Frontend/

- `Frontend/src/`：前端源码。
- `Frontend/src/assets/styles/`：全局与组件样式。
- `Frontend/src/components/`：跨页面组件（约束编辑器、顶栏、主布局）。
- `Frontend/src/router/`：路由与权限守卫。
- `Frontend/src/services/`：工作流/任务等 API 封装。
- `Frontend/src/stores/`：登录态、控制台等 Pinia 状态。
- `Frontend/src/utils/`：请求、画布、作者合同、执行展示等工具。
- `Frontend/src/views/constraint/`：约束管理页。
- `Frontend/src/views/dashboard/`：总览页。
- `Frontend/src/views/data/`：数据模板与记录管理页。
- `Frontend/src/views/device/`：设备模型、实例、适配器管理页及子组件。
- `Frontend/src/views/task/TaskList/`：任务列表、绑定、预检与运行抽屉。
- `Frontend/src/views/task/WorkflowDesigner/`：工作流画布设计器与检查器。
- `Frontend/src/views/user/`：登录注册与用户权限管理。
- `Frontend/tests/`：前端契约与结构测试。
- `Frontend/index.html` / `Frontend/src/main.js` / `Frontend/src/App.vue`：应用入口。
- `Frontend/vite.config.js` / `Frontend/package.json`：构建与依赖。

---

## Backend/

- `Backend/pom.xml`：Maven 工程与依赖。
- `Backend/src/main/java/`：Java 源码（见下一节逐文件）。
- `Backend/src/main/resources/application.yml`：端口、数据源、JWT、MQTT、引擎等配置。
- `Backend/src/main/resources/schemas/`：工作流、设备能力、状态机、约束、协议、系统执行等 JSON Schema。
- `Backend/src/main/resources/samples/`：适配器配置样例与开发说明。
- `Backend/src/main/resources/sql/`：权限表种子 SQL。
- `Backend/src/test/java/`：与主包结构对应的单元/接口测试（每个 `*Test.java` 测同名生产类）。
- `Backend/src/test/resources/`：测试资源（含 mockito 扩展）。

---

## Backend Java：`com.smartlab`

四层并列含义：

- `adapter`：对接设备适配器的 MQTT 与 Manifest，不负责工作流作者语言。
- `engine`：已落库模型的编译与运行，不负责自然语言生成。
- `global`：跨层枚举、Schema、安全、JSON 工具，不放厂商 LLM 客户端。
- `management`：HTTP、DTO、实体、Mapper、落库服务；工作流保存仍走这里。

若新增大模型生成，建议第五层 `com.smartlab.agent`：只产出 `WorkflowModelDocument`，再交给现有 `WorkflowService.saveDraft/publish`。

### 根

- `SmartLabApplication.java`：Spring Boot 启动类，扫描 Mapper 并打开定时任务。

### adapter/

- `AdapterManifestService.java`：解析并校验设备适配器能力清单（Manifest）。
- `MqttAdapterMessagingService.java`：MQTT 连接、订阅遥测/事件、下发命令的桥接服务。

### engine/connection/

- `InterfaceConnectionCatalog.java`：从已定稿模型查出节点/设备接口连了谁。
- `InterfaceConnectionEdge.java`：一条接口连接边的不可变描述。
- `InterfaceConnectionForwarder.java`：按活动任务把接口信号转发给对端节点或设备。
- `InterfaceConnectionIndex.java`：运行中接口连接的索引。
- `LiveInterfaceConnection.java`：绑定到具体任务步骤的一条活动接口连接。
- `PortConnectionEdge.java`：一条数据端口连接边。
- `PortConnectionIndex.java`：运行中端口连接的索引。
- `PortConnectionPuller.java`：按端口连接把源节点数据拉到目标节点。

### engine/constraint/

- `ConstraintAlertService.java`：约束告警的发出与记录入口。
- `ConstraintEngine.java`：约束求值引擎门面。
- `ConstraintEvaluationCoordinator.java`：组织一次约束评估要用哪些快照与规则。
- `ConstraintExpressionEvaluator.java`：约束/工作流表达式求值。
- `ConstraintMonitoringPlan.java`：一条运行时约束的监视计划。
- `ConstraintScopeSnapshotReader.java`：按约束作用域读取可观测量快照。
- `EffectiveConstraintModel.java`：编译后的有效约束模型。
- `EffectiveConstraintModelCompiler.java`：把库中规则编译成有效约束模型。
- `RuntimeConstraint.java`：一条可执行的运行时约束。
- `RuntimeConstraintKey.java`：运行时约束的定位键。

### engine/observation/

- `ObservableKey.java`：可观测量的复合主键（对象类型、流程、节点、变量等）。
- `ObservableSnapshotReader.java`：读取当前/历史观测快照的接口。
- `ObservableSnapshotService.java`：观测快照的统一读写服务。
- `ObservationHistoryStore.java`：观测历史环形/时序存储。
- `ObservationSample.java`：一个带时间戳的观测样本。
- `ObservationSnapshot.java`：某一可观测量的当前快照。
- `ObservationStatus.java`：观测状态枚举。
- `SnapshotOrigin.java`：快照来源（设备孪生、工作流、状态机等）。
- `device/DeviceTwinSnapshot.java`：设备数字孪生当前快照。
- `device/DeviceTwinSnapshotRegistry.java`：设备孪生快照注册表。
- `device/DeviceTwinSnapshotVersion.java`：孪生快照版本戳。
- `event/ObservableChangedEvent.java`：可观测量变化的内部事件。
- `event/ObservableTopologyChangedEvent.java`：可观测拓扑变化的内部事件。
- `statemachine/StateMachineObservationRegistry.java`：设备状态机相关观测量注册表。
- `workflow/WorkflowRuntimeSnapshotRegistry.java`：工作流运行时观测量注册表。

### engine/statemachine/

- `DeviceStateMachineRuntime.java`：一台设备实例上的状态机运行时。
- `IntrinsicConstraintMonitor.java`：设备状态机内建约束监视。
- `IntrinsicConstraintPlanRegistry.java`：内建约束监视计划注册表。
- `StateMachineCommandPort.java`：向状态机投递命令的端口。
- `StateMachineEngine.java`：设备状态机执行引擎。
- `StateMachineInterfaceOutputDispatcher.java`：状态机接口输出向外分发。
- `StateMachineInterfaceSignalEvent.java`：状态机接口信号事件。
- `StateMachineModels.java`：状态机编译/运行所用的内部模型。
- `StateMachineSendActionEvent.java`：状态机 SEND 动作要发往适配器的事件。
- `action/StateMachineActionExecutor.java`：状态机动作执行器接口。
- `action/StateMachineActionRegistry.java`：状态机动作执行器注册表。
- `action/SendStateMachineActionExecutor.java`：执行状态机 SEND 动作。

### engine/workflow/

- `DefaultWorkflowExecutionOperations.java`：工作流执行对库表的默认写操作实现。
- `WorkflowAssignmentExpression.java`：赋值表达式解析与求值辅助。
- `WorkflowConditionEvaluator.java`：触发器/分支条件求值。
- `WorkflowDefinitionCanonicalizer.java`：按系统模板覆盖节点骨架并保留业务项。
- `WorkflowDefinitionCompiler.java`：把模型文件编译成可执行图并做契约校验。
- `WorkflowEngine.java`：任务级工作流推进引擎。
- `WorkflowExecutionLogs.java`：执行日志写入辅助。
- `WorkflowExecutionOperations.java`：工作流执行读写库表的操作接口。
- `WorkflowInterfaceSnapshots.java`：节点接口当前值数组的读写边界。
- `WorkflowPortSnapshots.java`：节点端口当前值数组的读写边界。
- `WorkflowPreparation.java`：规范化结果、问题列表与编译结果的打包。
- `WorkflowTaskControlService.java`：任务启动/停止等控制入口。
- `WorkflowTaskLocks.java`：任务执行互斥锁。
- `WorkflowTaskPollScheduler.java`：轮询等待类触发器的定时调度。
- `WorkflowTriggerState.java`：节点触发器运行状态。
- `action/WorkflowActionExecutor.java`：工作流动作执行器接口。
- `action/WorkflowActionRegistry.java`：按动作名分发执行器。
- `action/WorkflowActionContext.java`：动作执行时的上下文。
- `action/WorkflowActionDefinition.java`：一条动作定义。
- `action/WorkflowActionResult.java`：动作执行结果。
- `action/WorkflowActionStatus.java`：动作执行状态。
- `action/WorkflowValueResolver.java`：从节点/变量解析动作参数值。
- `action/AssignWorkflowActionExecutor.java`：执行 ASSIGN 赋值动作。
- `action/CalculateWorkflowActionExecutor.java`：执行 CALCULATE 计算动作。
- `action/EmitSignalWorkflowActionExecutor.java`：执行 EMIT 发信号动作。
- `action/UpdateWorkflowActionExecutor.java`：执行 UPDATE 更新变量/生命周期动作。
- `action/WaitWorkflowActionExecutor.java`：执行 WAIT 等待动作。

### global/auth/

- `AuthenticationTokenService.java`：签发与解析 JWT。

### global/config/

- `ConstraintAuthorizationFilter.java`：约束相关接口的授权过滤。
- `JwtAuthenticationFilter.java`：请求上的 JWT 认证过滤。
- `MyBatisPlusConfig.java`：MyBatis-Plus 配置。
- `SecurityConfig.java`：Spring Security 链与放行规则。
- `TaskExecutionConfig.java`：任务执行线程/调度相关配置。
- `WorkflowEngineProperties.java`：工作流引擎可配置参数。

### global/contract/

- `AdapterOutboundSignal.java`：适配器出站信号名约定。
- `CommunicationProtocol.java`：通信协议枚举。
- `ConstraintControlSignal.java`：约束控制信号约定。
- `ConstraintOperator.java`：约束比较/逻辑运算符。
- `DataType.java`：统一数据类型枚举。
- `InterfaceType.java`：接口类型（工作流/状态等）。
- `ManualControlSignal.java`：人工点动控制信号约定。
- `MqttTopic.java`：MQTT 主题模板约定。
- `NodeLifecycleState.java`：工作流节点生命周期状态。
- `ObservableObjectType.java`：可观测量对象类型。
- `ProtocolContract.java`：协议字段与载荷约定。
- `SignalPayloadPolicy.java`：信号载荷策略。
- `StatusSignal.java`：状态类信号约定。
- `SystemExecutionContract.java`：系统执行规范（与 schema 对齐的代码侧合同）。
- `SystemViolationAction.java`：系统违约动作枚举。
- `TaskLifecycleState.java`：任务生命周期状态。
- `WorkflowControlSignal.java`：工作流控制信号约定。
- `WorkflowNodeActionType.java`：节点动作类型枚举。
- `WorkflowNodeFunctionType.java`：功能节点种类（START/END/BRANCH/AGGREGATE 等）。
- `WorkflowNodeSignal.java`：节点信号名约定。
- `WorkflowNodeSystemContract.java`：按节点类型生成系统骨架模板（含 `_system` 标记）。
- `WorkflowNodeType.java`：节点大类（功能/设备/子流程）。

### global/event/

- `ConstraintAlertEvent.java`：约束告警领域事件。
- `ConstraintRulesChangedEvent.java`：约束规则变更事件。
- `DeviceInstanceRetiredEvent.java`：设备实例注销事件。
- `DeviceInstanceSavedEvent.java`：设备实例保存事件。
- `DeviceModelSavedEvent.java`：设备模型保存事件。
- `DeviceTelemetryUpdatedEvent.java`：设备遥测更新事件。
- `TaskExecutionLogEvent.java`：任务执行日志事件。
- `TaskLifecycleObservationEvent.java`：任务生命周期观测事件。
- `WorkflowNodeObservationEvent.java`：工作流节点观测事件。

### global/protocol/

- `ProtocolDictionaryService.java`：协议字典（主题/字段）服务。
- `ProtocolTopicMatch.java`：MQTT 主题与字典项匹配。

### global/schema/

- `SchemaMetadataService.java`：把 resources/schemas 提供给前端的元数据服务。

### global/util/

- `JsonNodeSupport.java`：Jackson JsonNode 创建与转换辅助。
- `JsonPathReader.java`：按路径读取 JSON。

### management/controller/

- `constraint/ConstraintModelController.java`：有效约束模型查询 API。
- `constraint/ConstraintRuleController.java`：约束规则 CRUD API。
- `constraint/ViolationLogController.java`：违约日志查询 API。
- `protocol/ProtocolDictionaryController.java`：协议字典查询 API。
- `resource/adapter/AdapterIndexController.java`：适配器索引/注册信息 API。
- `resource/adapter/MqttBridgeController.java`：MQTT 桥状态与重连控制 API。
- `resource/data/DataIndexController.java`：数据集索引 API。
- `resource/data/DataRecordController.java`：数据记录读写 API。
- `resource/data/DataTemplateController.java`：数据模板 API。
- `resource/device/DeviceCategoryController.java`：设备分类 API。
- `resource/device/DeviceComponentController.java`：设备组件 API。
- `resource/device/DeviceInstanceController.java`：设备实例 API（含控制/孪生相关入口）。
- `resource/device/DeviceModelController.java`：设备模型 API。
- `resource/device/DeviceTwinStateController.java`：设备孪生状态查询 API。
- `resource/device/PropertyTypeController.java`：属性类型 API。
- `resource/device/ResourceStructureController.java`：实验室资源拓扑 API。
- `resource/scene/SceneController.java`：场景 API。
- `schema/SchemaMetadataController.java`：前端所需 Schema 元数据 API。
- `user/PermissionController.java`：权限管理 API。
- `user/UserController.java`：登录、当前用户与用户管理 API。
- `workflow/FlowNodeController.java`：流程节点查询 API。
- `workflow/TaskController.java`：任务创建、预检、启动与监控 API。
- `workflow/WorkflowController.java`：工作流模型列表/详情/导出/草稿/发布 API。

### management/dto/

- `common/ApiResponse.java`：统一 HTTP 响应包装。
- `common/PageResult.java`：分页结果包装。
- `constraint/ConstraintDTO.java`：约束规则传输对象。
- `constraint/ConstraintModelRuleSummary.java`：约束模型中规则摘要。
- `constraint/EffectiveConstraintModelView.java`：有效约束模型给前端的视图。
- `resource/adapter/AdapterRouteDTO.java`：适配器路由信息。
- `resource/data/DataTemplateDTO.java`：数据模板传输对象。
- `resource/data/DataTemplateSaveDTO.java`：数据模板保存请求。
- `resource/device/DeviceInstanceDTO.java`：设备实例传输对象。
- `resource/device/DeviceModelDTO.java`：设备模型传输对象。
- `resource/device/DeviceModelSaveDTO.java`：设备模型保存请求。
- `user/MenuDTO.java`：菜单项传输对象。
- `user/UserRequestDTO.java`：用户创建/更新请求。
- `workflow/DeviceBindingRequirement.java`：任务绑定设备时的一个槽位需求。
- `workflow/TaskCreateRequest.java`：创建任务请求。
- `workflow/TaskDeviceBindingRequest.java`：任务设备绑定请求。
- `workflow/TaskExecutionView.java`：任务执行总视图。
- `workflow/TaskMonitorSummary.java`：任务监控摘要。
- `workflow/TaskNodeExecutionView.java`：单个节点执行视图。
- `workflow/TaskPreflightRequest.java`：任务预检请求。
- `workflow/TaskPreflightResponse.java`：任务预检结果。
- `workflow/WorkflowDetailResponse.java`：从两张表拼回的内部读模型（含 version/status）。
- `workflow/WorkflowDTO.java`：旧的工作流扁平传输对象（少量遗留）。
- `workflow/WorkflowIssue.java`：规范化/编译/发布问题项。
- `workflow/WorkflowModelDocument.java`：与 `工作流模型.json` 对齐的模型文件对象。
- `workflow/WorkflowModelDocuments.java`：模型文件与读模型/列表/导出之间的转换。
- `workflow/WorkflowModelMetadata.java`：模型文件头（flowModelId/name/description）。
- `workflow/WorkflowPreparationApiResponse.java`：草稿/发布接口返回给前端的准备结果。
- `workflow/WorkflowPreparationResponse.java`：服务层准备结果（含内部读模型）。
- `workflow/WorkflowResourceRequirementsResponse.java`：发布后设备绑定需求汇总。
- `workflow/WorkflowSuccessorConflict.java`：已有后继版本时的冲突信息。
- `workflow/WorkflowSummaryResponse.java`：工作流列表项。
- `workflow/WorkflowViewResponse.java`：详情读取：模型文件字段加上库中的 version/status 等。

### management/entity/

- `constraint/ConstraintRule.java`：约束规则表实体。
- `constraint/ViolationLog.java`：违约日志表实体。
- `resource/adapter/AdapterIndex.java`：适配器索引表实体。
- `resource/data/DataIndex.java`：数据集索引表实体。
- `resource/data/DataTemplateDetail.java`：数据模板明细表实体。
- `resource/data/DataTemplateMain.java`：数据模板主表实体。
- `resource/device/DeviceCategory.java`：设备分类表实体。
- `resource/device/DeviceComponents.java`：设备组件表实体。
- `resource/device/DeviceInstanceLifecycle.java`：设备实例生命周期辅助。
- `resource/device/DeviceInstances.java`：设备实例表实体。
- `resource/device/DeviceModels.java`：设备模型表实体。
- `resource/device/DeviceTwinStates.java`：设备孪生状态表实体。
- `resource/device/PropertyType.java`：属性类型表实体。
- `resource/device/ResourceStructure.java`：实验室空间拓扑节点实体。
- `resource/scene/SceneDetail.java`：场景明细实体。
- `resource/scene/SceneMain.java`：场景主表实体。
- `user/PermissionInfo.java`：权限信息表实体。
- `user/UserInfo.java`：用户信息表实体。
- `workflow/ExecutionLog.java`：执行日志表实体。
- `workflow/FlowModels.java`：工作流头表实体（名、版本、状态、连线 JSONB、节点引用）。
- `workflow/FlowNode.java`：工作流节点表实体。
- `workflow/Task.java`：任务实例表实体。
- `workflow/TaskStep.java`：任务步骤表实体。

### management/mapper/

- `common/PostgresJsonbTypeHandler.java`：PostgreSQL JSONB 与 JsonNode 的类型处理器。
- 其余 `*Mapper.java`：对应同名实体的 MyBatis-Plus Mapper（`AdapterIndex`、`DataIndex`、`DataTemplate*`、`Device*`、`PropertyType`、`ResourceStructure`、`Scene*`、`PermissionInfo`、`UserInfo`、`ConstraintRule`、`ViolationLog`、`ExecutionLog`、`FlowModels`、`FlowNode`、`Task`、`TaskStep`）。

### management/service/

- `db/common/ManagementCrudService.java`：管理端通用 CRUD 基类。
- `db/constraint/ConstraintRuleService.java`：约束规则持久化与查询。
- `db/constraint/TaskConstraintService.java`：任务运行期约束装配。
- `db/constraint/ViolationLogService.java`：违约日志持久化与查询。
- `db/resource/adapter/AdapterIndexService.java`：适配器索引持久化与查询。
- `db/resource/data/DataIndexService.java`：数据集索引服务。
- `db/resource/data/DataRecordService.java`：动态数据记录服务。
- `db/resource/data/DataTemplateService.java`：数据模板服务。
- `db/resource/data/PropertyTypeService.java`：属性类型服务。
- `db/resource/device/DeviceCategoryService.java`：设备分类服务。
- `db/resource/device/DeviceComponentService.java`：设备组件服务。
- `db/resource/device/DeviceInstanceService.java`：设备实例生命周期与绑定。
- `db/resource/device/DeviceModelService.java`：设备模型保存与查询。
- `db/resource/device/DeviceTwinStateService.java`：设备孪生状态读写。
- `db/resource/scene/ResourceStructureService.java`：资源拓扑服务。
- `db/resource/scene/SceneService.java`：场景服务。
- `db/user/CurrentUserPermissionService.java`：当前登录用户权限解析。
- `db/user/PermissionService.java`：权限数据维护。
- `db/user/UserService.java`：用户注册登录与资料。
- `db/workflow/ExecutionLogService.java`：执行日志查询服务。
- `db/workflow/FlowNodeService.java`：流程节点查询服务。
- `db/workflow/TaskExecutionViewService.java`：组装任务执行监控视图。
- `db/workflow/TaskService.java`：任务创建与状态管理。
- `db/workflow/WorkflowExecutionReadinessService.java`：启动前设备/适配器就绪检查。
- `db/workflow/WorkflowRuntimeService.java`：任务运行时对步骤/日志的管理侧操作。
- `db/workflow/WorkflowService.java`：工作流模型文件的规范化、拆表保存、拼回与编译入口。
- `db/workflow/WorkflowSuccessorExistsException.java`：存在后继版本时抛出的冲突。
- `db/workflow/WorkflowTaskResourceService.java`：从流程树收集设备绑定需求并校验实例。
- `menu/MenuCatalogService.java`：静态菜单目录。
- `menu/MenuService.java`：按权限过滤后的菜单树。
- `protocol/AdapterPayloadMapperService.java`：适配器载荷与内部协议字段映射。

### management/sse/

- `DeviceConsoleSseHub.java`：设备控制台 SSE 推送枢纽。
- `TaskExecutionSseHub.java`：任务执行 SSE 推送枢纽。

### 测试 Java（`src/test/java`）

每个 `*Test.java` 对应上述同名生产类的单元或接口测试；包路径与 `src/main/java` 对齐（如 `engine/workflow/WorkflowDefinitionCompilerTest.java` 测编译器）。另有 `global/contract/SchemaContractConformanceTest.java` 检查代码合同与 `resources/schemas` 一致，`adapter/TestAdapterIniContractTest.java` 检查测试适配器 ini 与契约一致。
